# Exported workflows

- `CODEX-TEST-MCP-Sandbox.json` — existing MCP integration sandbox; preserved
  unchanged.
- `CODEX-TEST-AI-Strategy-Factory-v0.1.json` — inactive AI Strategy Factory
  synthetic example/manual/JSON intake, human review, and approved-artifact
  vertical slice.

Never activate or publish a workflow without explicit approval.
